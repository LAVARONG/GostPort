# 更新日志 (Changelog)

所有针对平台后端的 API 更新、前端 UI 重构或重大 Bug 修复都将详细记录于此。遵循倒序时间轴排版规范。

---
## [2026-03-24] - 品牌命名重置：GostPort ✨
- **全案重构**：为契合项目“极简留白、纯粹管道”的第一性原理精神，正式剥离原有的臃肿名称 `gost-manager`。
- **改动范围**：
  1. 重塑前端 UI 品牌标识及 HTML 标题。
  2. 收敛后端 Go 模块名、系统启动 Banner 等特征。
  3. 各环境编译产出（`go build -o gostport`）、`.gitignore` 过滤项及配套文档全部同步变更。

## [2026-03-24] - Github 开源规范化整理 📦
- **配置与脱敏**：
  1. 增加 `.gitignore` 以隔离编译产物与含有隐私节点数据的 `config.json`。
  2. 新增 `config.example.json` 作为面板配置结构公开参考指引。
- **文档与协议**：
  1. 重构 `README.md` 以符合 GitHub 极简开源标准，增加徽章与说明。
  2. 采用极简的 `MIT License` 声明项目授权，版权所有 `LAVARONG`。

## [2026-03-24] - 初始架构构建 🏅
- **初版发布**：引入 Gost 端口控制器的核心服务与面板。
- **功能特性**：
  1. 采用 Golang 构建无依赖多线程进程管理服务端。
  2. 实现以 `config.json` 为持久化核心的极简状态机。
  3. 采用 Vue3 (Petite) + Tailwind 融入“暗黑留白”第一性原理视觉设计。
- **详见文档**：[`/docs/2503/24API构建.md`](./2503/24API构建.md) - 对于接口和代码架构的首次详述。
